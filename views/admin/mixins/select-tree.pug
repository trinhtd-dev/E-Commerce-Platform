mixin select-tree(items, level = 1, parent = "")
    each item in items
        option(
            value= item.id
            selected = item.id ==  parent ? true : false
        )= `${'-- '.repeat(level)} ${item.title}`
        if(item.children && item.children.length)
            +select-tree(item.children, level + 1, parent)